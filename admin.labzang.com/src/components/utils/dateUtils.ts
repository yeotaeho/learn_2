/**
 * @deprecated 이 파일은 lib/utils/dateUtils.ts로 이동되었습니다.
 * 새로운 코드에서는 lib에서 import하세요:
 * import { getLocalDateStr, getDayOfWeek } from '@/lib';
 */

// 하위 호환성을 위해 lib에서 re-export
export { getLocalDateStr, getDayOfWeek } from '../../lib/utils/dateUtils';

